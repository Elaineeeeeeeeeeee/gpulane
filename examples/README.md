# Examples

This directory contains minimal reference files for first-time users evaluating `gpulane`.

- `minimal/` shows a repo-local manifest for one training task.
- `batch/` shows a small batch file that applies config overrides.

These examples are intentionally small and do not assume a specific model codebase.
